# inflacion-personal-ar

Aplicación Shiny mínima para estimar el valor ajustado por inflación de un monto.

## Ejecutar

1. Instalar dependencias:
   `install.packages("shiny")`
2. Ejecutar la app:
   `shiny::runApp()`